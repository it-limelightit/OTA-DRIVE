server {
  listen 80;
  server_name _;
  root /usr/share/nginx/html;
  index index.html;
  location /api/ { proxy_pass http://backend:3000; }
  location /api/device-events { proxy_pass http://backend:3000; proxy_buffering off; proxy_cache off; proxy_read_timeout 1h; }
  location /health { proxy_pass http://backend:3000; }
  location / { try_files $uri $uri/ /index.html; }
}
